image: node

before_script:
  - yarn install

cache:
  paths:
    - node_modules/

pages:
  script:
    - yarn generate
    - mkdir public
    - mv dist/* public/
    - cp -r static/.well-known public/
  artifacts:
    paths:
      - public
  only:
    - master
