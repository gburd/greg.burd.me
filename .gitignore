# Build output
public/

# Nix
.direnv/
result
result-*

# Editor / local state
.nvimlog

# Local Netlify CLI state (contains site ID; safe to commit but kept local)
.netlify/

# Pre-commit config is generated by Nix flake; symlinked at dev shell entry
.pre-commit-config.yaml

# Stray build output
site/

# External review doc (local only)
greg-burd-me-review.md
